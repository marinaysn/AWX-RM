import React from 'react';
import './Toolbar.css';
import  Logo from '../../Logo/Logo'
const Toolbar = (props) => {
    return (
        <header className="Toolbar">
            
            <Logo />
            <div>Menu</div>
            <nav>...</nav>
        </header> ) 
}

export default Toolbar;
